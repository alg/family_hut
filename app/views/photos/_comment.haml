.comment
  .avatar= image_tag(comment.user.avatar.url(:small))
  .details
    .head
      %span.name= comment.user.name
      %small (#{time_ago_in_words(comment.created_at)})
    .body= comment.comment
