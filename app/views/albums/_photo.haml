.photo{ :pid => photo.id, :class => owns?(photo) ? 'editable' : nil }
  .title= photo.title
  .date= photo.created_at.strftime("%b %e, %Y")
  .image= link_to image_tag(photo.image.url(:brief)), [ photo.album, photo ]
